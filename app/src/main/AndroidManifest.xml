<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logomdi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logomdi"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal">
        <activity android:name=".Consulta"
            android:parentActivityName=".Lista"></activity>
        <activity android:name=".Historico"
            android:parentActivityName=".Lista"/>
        <activity android:name=".Hemoglobina"
            android:parentActivityName=".Lista"/>
        <activity android:name=".IngresarDatos"
            android:parentActivityName=".Lista"/>
        <activity android:name=".Lista" />
        <activity android:name=".Register"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/splashScr">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>